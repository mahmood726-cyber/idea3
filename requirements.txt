# Dependencies for the canonical, self-contained analysis
# (causal_forest_analysis.py) and its test suite (test_causal_forest.py).
# Major versions pinned for reproducibility.
numpy>=1.24,<3
scipy>=1.10,<2
pandas>=2.0,<3
scikit-learn>=1.3,<2
matplotlib>=3.7,<4
seaborn>=0.12,<1

# The optional, heavier revisions
#   causal_forest_meta_analysis_CORRECTED.py
#   causal_forest_FINAL_PUBLICATION_READY.py
# additionally require econml and shap (see requirements_fixed.txt).
# They are NOT needed for the canonical script or the tests.
